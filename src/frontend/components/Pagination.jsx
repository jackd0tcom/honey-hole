const Pagination = ({ pages }) => {
  return (
    <div className="hh-pagination-wrapper">
      {/* {pages.forEach((page) => {
        return <button>{page}</button>;
      })} */}
    </div>
  );
};

export default Pagination;
